# Core — stdlib only needed to collect; these add power but collector works without them
requests>=2.31.0

# Storage / analysis
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
duckdb>=0.9

# Backtest / ML (optional)
scikit-learn>=1.3
matplotlib>=3.7

# Dev
pytest>=7.0
python-dotenv>=1.0
